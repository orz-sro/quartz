![[Pasted image 20260311205147.png|1000]]      





##### 数据结构就三个 :  逻辑结构、存储结构  和  数据运算  







> [!error] 数据结构
>
> ![[Pasted image 20260311210318.png|500]]
>
> 数据的 **<font color="#4bacc6">逻辑结构</font>** 是从面向实际问题的角度出发的，只**采用抽象表达**方式，独立于存储结构，数据的存储方式有多种不同的选择；
>
> 而数据的 **<font color="#e36c09">存储结构</font>** 是逻辑结构在计算机上的映射，它不能独立于逻辑结构而存在。
>
> 数据结构包括三个要素（逻辑结构 存储结构 数据运算），缺一不可。







##### 有效 算法的五个重要特性(recite)：有穷性   确定性   可行性  输入  输出



![[Pasted image 20260312194412.png]]

#### 上题答案为C；



D选项的正确表述：我们可以利用==正确性==、==可读性==、==健壮性==以及==时间效率==与==空间效率==来衡量算法的优劣







> [!todo] 时间复杂度例题：
>
> ![[Pasted image 20260312183636.png]]
>
> 本题是特殊的多个代码段组成的嵌套代码，但是计算 $O(n)$ 不能使用乘法规则因为嵌套代码频度受上式变量制约。
>
> 需要看$\log_{2}n$个二级代码`for(int j = 0; j < 1; j++)`被执行次数的累加
>
> 这里使用等比数列求和，有时候可能只需要高斯求和
>
> ![[Pasted image 20260312183607.png]]
>
> 第三行代码`sum += ++i;`等同于 `sum = sum + (++i);`

利用高斯公式可以得到频度公式就是 $1+2+3+……+k=\frac{k(1+k)}{2}=n$
